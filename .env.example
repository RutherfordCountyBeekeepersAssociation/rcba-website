# Public Square payment links are stored in src/lib/payment-links.ts
# so they are easy to find and update without touching individual components.
# No private API keys are needed for Square payment links.
